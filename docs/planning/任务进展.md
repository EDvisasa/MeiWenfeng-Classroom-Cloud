# 媚吻锋随身课堂 —— 任务进展与待办清单

本文件记录了项目当前的真实开发进度与未完成的核心任务，以便快速了解工程现状，避免重复分析。

## ✅ 已完成的核心内容 (MVP 第一阶段)
- [x] 初始化项目结构，建立 Git 仓库，编写 `.gitignore` 和 `.env.example`
- [x] 搭建 FastAPI 后端与 SQLite 数据库（含核心数据表结构）
- [x] 搭建 React 前端三栏 IDE 布局（深色主题，支持响应式布局）
- [x] 成功接入大语言模型流式对话（已支持模型动态切换）
- [x] 实现了前端特有的动作描写样式（斜体灰字渲染）
- [x] 编写 `start.bat` 一键启动脚本
- [x] 记忆压缩逻辑部分打通（`memory_decay.py` 及遗忘曲线算法初探）
- [x] 前端组件化架构重构（将主入口大文件 `App.jsx` 拆分为 `FileTree`、`ChatPanel`、`StatusPanel` 及 `CropModal` 组件）
- [x] 本地真实文件挂载（支持自定义路径、刷新、一键同步、指示灯状态轮询及 VS Code 风格文件树）
- [x] API 动态配置与酒馆式设置面板（对接本地 Node.js 反代项目 `antigravity2api-nodejs`，支持子模型获取、保存及激活，并将字体大小改造成下拉设置菜单）

## ✅ 最新完成内容 (2026-06-12 会话)
- [x] **修复本地模型连接 Connection error**：`model_router.py` 中对 `localhost` 自动映射为 `127.0.0.1`，解决 Windows 下 httpx 走 IPv6 导致的连接拒绝问题（同样适用于 `chat.py` 的 `/models/available` 代理接口）
- [x] **补全缺失的后端 API**：实现了 `POST /api/chat/models/update`（保存 API 配置）和 `GET /api/chat/models/available`（获取可用子模型列表）两个此前遗漏的端点
- [x] **子模型输入框改造**：将下拉 `<select>` 改为带 `<datalist>` 联想提示的 `<input>` 输入框，支持手动输入无法枚举模型列表的公益站/第三方 API 模型名
- [x] **RAG 多后端切换系统**：引入 `rag_factory.py` 工厂模块，支持三档知识库后端一键切换：
  - 🚀 **ChromaDB（默认）**：本地向量库，零外部依赖，轻量高效
  - ☁️ **RAGFlow**：保留原有完整接入，需 Docker 服务在线
  - 🔗 **外部 API**：预留 Dify、FastGPT 等第三方 RAG 服务接口
- [x] **记忆系统语义升级**：ChromaDB 模式下，每次对话结束后自动将对话向量化存入本地 `conversation_memory` Collection；每次发消息时同时做知识库检索 + 对话记忆语义检索，替代原有低效的 RAGFlow 关键词搜索
- [x] **前端知识库配置 UI**：在右上角 ⚡ API 配置面板底部新增 📚 知识库后端 三档切换按钮，RAGFlow/外部 API 模式下展开对应的地址和密钥输入框

## ✅ 最新完成内容 (2026-06-13 会话)
- [x] **修复 ChromaDB 并发初始化冲突**：为本地 ChromaDB 客户端增加 `threading.Lock` 线程锁，彻底解决了因为 `sentence-transformers` 首次拉取模型耗时较长引发的知识库错误初始化与多线程并发竞态问题（500 Internal Server Error）。
- [x] **补全知识库后端解耦逻辑**：清理并修复了 `/api/chat/files/sync` (单文件同步) 与 `memory_decay.py` (记忆总结算法) 中遗漏的 `ragflow_client` 硬编码调用 Bug，确保全量功能均严格使用 `get_rag_client()` 动态分发。
- [x] **内置数据库监控大屏**：新增 `DatabaseViewer.jsx` 前端组件和配套的只读路由接口 `db_viewer.py`，支持在浏览器端实时审查底层 SQLite 结构和所有系统记录数据，打通“黑盒开发”的壁垒。
- [x] **记忆归档算法重构**：优化大模型记忆归档提示词算法，引入严格的**“时间跨度四段式”**（时间、核心词、一句话总结、详细描述）规范，并通过脚本提取时间戳直插 Prompt 解决模型自发幻觉引发的时间线错乱丢失问题。
- [x] **老旧数据清洗合并**：开发 `migrate_memory.py` 清洗脚本，全自动完成旧版冗余或混乱 `Level 1`、`Level 2` 数据的四段式重排版处理，并将规范化后的新数据无缝同步入 ChromaDB 的 `Memory_Knowledge` 集合。
- [x] **对话流底层 RAG 指示灯**：重构了 `rag_client.retrieve` 使其直接返还切片数组用于精准分类计数，在每次响应生成**前**利用 SSE 回传独立角色 `system_info` 数据包，以居中悬浮框形式清晰告知用户本次检索调用了多少份“讲义片段”与“长期记忆”。

- [x] **后端路由与服务层模块解耦**：将原来超长的 `routers/chat.py` 成功拆分出了 `memory.py`、`rag.py`、`course.py`、`db_viewer.py` 等独立路由，并在 `services` 层建立了 `slash_handler.py` 专门负责不同斜杠指令 of 后端分发。

## ✅ 最新完成内容 (2026-06-14 会话)
- [x] **对话框编辑铅笔按键位置重构**：将用户气泡的编辑铅笔按钮（`.edit-msg-btn`）从消息头部发件人栏移动到用户消息气泡的右下角，通过 `position: absolute` 定位并增加优雅的悬浮态过渡效果，有效避免了布局冲突，且操作逻辑更加直观。
- [x] **UI 主题与背景图片自定义**：
  - 支持用户上传自定义背景图片，并缓存于 `localStorage`，支持一键恢复默认背景。
  - 新增毛玻璃（Glassmorphism）特效开关，支持通过 `.disable-blur` 彻底关闭毛玻璃效果以提升低配渲染性能。
  - 优化了 `.theme-dark` 暗色模式下的配色，包含雅致的紫色动作描写字色与粉色文字强调色，全面提升视觉高级感。
- [x] **高级 UI 细节微调与控制器**：
  - 新增用户名字大小调节滑动条（`10px ~ 24px`），通过 CSS 变量 `--name-font-size` 实时生效。
  - 将设置面板宽度由 `260px` 拓展至 `320px`，优化了排版与行距。
  - 重构了右侧导师与大纲状态栏（`StatusPanel`）的头部设计，将原本隐藏在下拉菜单里的“导师人设（原版/精简）”一键切换按钮直接外置到了状态栏头部，并新增了书本图标与导师选择器占位符。
  - 修复了主内容区在特定窗口高度下的内容截断问题（为各层 panel 和 container 增加了 `min-height: 0` 防止 Flex 伸缩截断）。
  - 对 VS Code 环境下的布局（间距、边角半径、外边距、输入框宽度及图标位置）进行了精细调整，使其能完美嵌入 IDE 侧边栏。
- [x] **UI 全面优化与深度打磨**：
  - 彻底重构深色模式（Dark Mode）色彩调色板，增强文本可读性与界面的高级感。
  - 修复聊天面板中的硬编码白色背景问题，系统提示词文字颜色调整为清爽的蓝色以提升辨识度。
  - 修复右侧面板高度无法与主聊天面板对齐的问题，使其完美贴合。
  - 优化导师状态面板内部元素的间距，提升信息密度与视觉整洁度。
  - 更新并优化了消息操作按钮（编辑、删除、新建等）的 SVG 图标，将导师无头像时的默认展示修改为“师”字图标，强化沉浸式体验。

## ✅ 最新完成内容 (2026-06-15 会话)
- [x] **默认头像优化**：将导师默认图片更新为更具代表性的“博士帽” SVG 图标，学生默认图像更新为“阅读书籍”的人像图标。
- [x] **UI 图标对齐与细节优化**：去除了界面特定按钮多余的文本仅保留图标；统一将 API/UI 相关的设置图标调整为“插头”以增加语义清晰度，修复了多个小图标的不对齐和偏移动位问题。
- [x] **安全局域网访问 (防肉鸡)**：
  - 将 FastAPI 后端与 Vite 前端开发服务器统一绑定到 `0.0.0.0` 开启全量监听，支持局域网移动端访问。
  - **核心安全层**：参考 SillyTavern 架构，在后端增加 `IPWhitelistMiddleware`。只允许本机 (`127.0.0.1`) 及局域网内网 IP (`192.168.x.x`、`10.0.0.0/8` 等) 发起连接。公网及不明扫描一律直接拒绝 `403 Forbidden`。
- [x] **前端 API 动态寻址重构**：创建全局 `config.js` 托管动态 `API_BASE`，全面清理之前在 `App.jsx`、`FileTree.jsx` 和 `DatabaseViewer.jsx` 等组件中写死的 `http://localhost:12701`。这确保了在电脑与手机跨屏访问时均能正常进行接口通信。
- [x] **页面标题修改**：将浏览器页签标题从“媚吻锋的随身课堂”精简为“随身课堂”。

## ✅ 最新完成内容 (2026-06-17 会话)
- [x] **全量架构深度验证与安全审计**：使用 Agent 工具流对核心系统进行了深度的架构与安全测试，并生成了全中文的架构审查报告 `docs/architecture_verification_report.html`。
- [x] **修复 SlashHandler 路径穿越漏洞**：在处理 `<explainer>` 标签生成文档时，通过 `os.path.basename()` 与 `os.path.abspath` 双重检查，彻底堵死了通过 `../` 越权写入系统文件的可能。
- [x] **修复记忆衰减并发竞争条件**：在 `memory_decay.py` 中引入 `threading.Lock()`，修复了在高频对话流中触发合并时，因网络 I/O 阻塞导致的重复抽取日志的并发冲突问题。
- [x] **加固 BashTool 物理护栏**：摒弃了脆弱的字符串屏蔽法，使用 `shlex.split` 进行 AST 级别的二进制指令拦截，全面阻断了 `tail`, `head`, `more` 以及 `c\at` 等 Shell 转义绕过手段。
- [x] **修复 API 安全中间件漏洞**：
  - 修正了 `CORS` 配置，限制为确切的信任端口，移除了危险的 `allow_origins=["*"]` 混用 `credentials=True` 问题。
  - 在 `ip_whitelist_middleware` 中增加判空检查，解决了特定 ASGI 环境下 `request.client` 为空导致的 500 DoS 崩溃。
- [x] **修复前端 UI 视觉缺陷**：
  - 修复了 `blockParser.js` 破坏文本前导空格和换行的问题（改用正则表达式内部的 `\s*` 代替链式的 `.trim()`）。
  - 修复了 `cropImage.js` 中图片旋转时包围盒未重算导致的切边裁剪 Bug（引入三角函数动态计算宽高）。
- [x] **测试用例沉淀**：将上述核心后端安全漏洞全部编写成标准的 pytest 测试用例，统一存放在 `backend/tests/` 目录中，确保后续修改不会倒退。

## ✅ 最新完成内容 (2026-06-18 会话)
- [x] **记忆衰减系统架构重构**：将原本直接在 `ResponsePipeline` 中启动裸线程 (`threading.Thread`) 执行的 `process_memory_decay` 逻辑，重构为标准的 `MemoryDecayHandler` 类。
- [x] **副作用管线完全解耦**：记忆压缩触发逻辑现已通过 `ActionRegistry` 统一派发，消除了背景线程绕过全局异常处理的风险，符合 `CONTEXT.md` 中定义的架构规范。
- [x] **路由层代码整洁化**：`/api/chat/force_decay` 和 `/api/chat/summarize` 接口剥离了底层的领域逻辑导入，转而通过注册表 (`action_registry.get_handler("memory_decay")`) 触发压缩任务。
- [x] **TDD 自动化测试**：新增了 `test_memory_decay_handler.py` 垂直切片测试，确保重构后的压缩逻辑行为正确。

## 🚀 即将开展 (下一阶段核心目标)
当前前端 UI 与底层知识库/多模型路由基建已基本打磨完毕。项目正式步入 **第三阶段：课程与记忆系统（课堂引擎）**。
## ⚠️ 当前遗留问题与待办任务

### 1. 课程系统与授课流控 (中优先级，第三阶段)
- **现状**：当输入 `/计划` 或 `/上课` 时，仅前端模拟了一条系统回复文本，后端并无实际课程状态切换。
- **待办**：基于 SQLite 的 `course_progress` 表，实现基于学习进度的课程大纲动态生成，并在特定命令下切入真正的"考核模式"。

### 2. UI 主题自定义拓展 (低优先级，第四阶段)
- **现状**：完成了基本的主题颜色优化与背景上传/毛玻璃特效控制，但尚未提供完全独立的换肤文件。
- **待办**：建立完整的多皮肤切换体系，丰富用户的界面定制选择。

---
> **开发者提示**：在开始新的开发工作前，请先检查「首次启动 ChromaDB 嵌入模型下载」项，确保网络畅通后再重启后端。后续优先从**课程系统与授课流控**或**记忆衰减/归档脚本**切入。
